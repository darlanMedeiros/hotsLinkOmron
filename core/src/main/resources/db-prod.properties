# Ajuste para o banco de producao
# Recomenda-se sobrescrever via variaveis de ambiente DB_URL/DB_USER/DB_PASSWORD

db.url=jdbc:postgresql://prod-db:5432/omron?currentSchema=public
db.user=omron_user
db.password=change-me
